# Dev/test only. requirements.txt stays RUNTIME-ONLY so Streamlit Cloud's cold
# start doesn't install a test framework it will never run.
-r requirements.txt
pytest>=8.0
pyarrow>=15.0        # parquet caches (tools/ and the Lab)
