# Dev/test only — never installed by Streamlit Cloud.
-r requirements.txt
pytest~=8.0
pyarrow~=23.0        # parquet caches used by tools/ and ad-hoc analysis
watchdog~=6.0        # faster `streamlit run` auto-reload while developing
